'use strict';

module.exports = {
  // Server port for the consumer
  consumerPort: 8000,

  // Server host for the consumer
  consumerHost: 'localhost',

  // Poll frequency for consumer job processing
  consumerFrequency: 10,

  // Generator frequency for new expression creation
  generatorFrequency: 1000,
};
